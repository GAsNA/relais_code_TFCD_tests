Assignment name    :    sort_int_tab
--------------------------------------------------------------------------------------

#include <stdio.h>

int    *sort_int_tab(int *tab);

int main()
{
    int tab[5] = {6, 5, 7, 42, 0, -61};
    int *n_tab = sort_int_tab(tab);
    int i = -1;
    while (++i < 5)
        printf("%i\n", n_tab[i]);
    return (0);
}

int main()
{
    int *tab = sort_int_tab(NULL);
    return (0);
}
